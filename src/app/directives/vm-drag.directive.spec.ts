import { VmDragDirective } from './vm-drag.directive';

describe('VmDragDirective', () => {
  it('should create an instance', () => {
    const directive = new VmDragDirective();
    expect(directive).toBeTruthy();
  });
});
